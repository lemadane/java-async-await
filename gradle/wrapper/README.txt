Run the following once on a machine with Gradle installed to generate the official wrapper JAR and scripts:

gradle wrapper --gradle-version 9.6.1

The wrapper properties file is already pinned to Gradle 9.6.1.
